{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "main": "index.js",
  "scripts": {
    "serve": "node index.js"
  },
  "keywords": [<%- keywords %>],
  "author": "<%= author %>",
  "license": "<%= license %>",
  "dependencies": {
    "koa": "^2.13.4"
    <% if (middleware.router) { %>
    ,"koa-router": "^10.1.1"
    <% } %>
    <% if (middleware.views) { %>
    ,"koa-views": "^8.0.0",
    "pug": "^3.0.2"
    <% } %>
    <% if (middleware.body) { %>
    ,"koa-body": "^4.2.0"
    <% } %>
    <% if (middleware.static) { %>
    ,"koa-static": "^5.0.0"
    <% } %>
  }
}
